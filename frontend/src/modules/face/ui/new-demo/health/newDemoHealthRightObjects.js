import { NEW_DEMO_CD_OBJECT_TWIN_NAMES } from "../../../../../core/data/static/newDemoCdObjectTwins.js"

/**
 * Объекты для правой розы «2. Цифровые двойники объектов» (new-demo).
 * Имена совпадают с уровнем «ЦД объекта» гиперкуба (`funnelEntities.OBJECTS`).
 * Проценты value/coverage задаются в useFacePageModel (45–80% от faceSeed).
 */
export const NEW_DEMO_HEALTH_RIGHT_OBJECTS = NEW_DEMO_CD_OBJECT_TWIN_NAMES.map((name) => ({ name }))
