# Formalizator JSON (перенос)

Канонические JSON для сборки Vite перенесены в **`../prompt-pipeline/`** (см. `docs/prompt-pipeline-handoff`). Импорты в коде указывают на `prompt-pipeline`, эта папка оставлена для совместимости путей в документации.
